/**
 * The output styles and their content shapes, re-exported.
 *
 * They live under `api/_lib` so the serverless function can resolve everything
 * it imports from inside `api/`; the app reads them from here.
 */
export * from '../../../api/_lib/styles'
